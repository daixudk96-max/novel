# Progress

- Collected high-accuracy reviews from explore, oracle, and librarian.
- Verified oracle finding that `tests/test_cli_e2e.py -k "chapter and draft"` selects 0 tests.
- Preparing a single-file plan-tightening delegation before any implementation work starts.
- Tightened `changes/roadmap-next-step/tasks.md` for a High Accuracy start: Task 1 is now contract-freeze only, the retry contract is explicit, the retry boundary forbids wrapping file/state side effects, and exhausted-retry plain/JSON behavior is frozen.
- Replaced the zero-match E2E proof hole in the plan with guarded exact test names/node commands and expanded Task 5 doc-sync scope to cover every stale truth-bearing Route A readiness doc.
- Clarified that Task 4 packaged verification must run through the packaged Python 3.12 `novel.exe` entrypoint with the existing offline harness.
- Implemented Task 2 scope only: created `novel_runtime.llm.resilience` with explicit retry classification, deterministic provider-call backoff, and the future wiring hook for provider SDK retries set to zero.
- Added drafter-focused resilience tests in `tests/test_drafter.py` and verified `rtk py -3.12 -m pytest tests/test_drafter.py -k "resilience or retry or rate_limit or provider"` passes (`12 passed, 8 deselected`).
- Wired Task 3 through `provider.py`, `drafter.py`, and `chapter.py`: Route A retries now wrap only the provider invocation, side effects remain after successful draft completion, and exhausted retries emit the frozen plain/JSON error contract.
- Added retry-focused CLI coverage in `tests/test_cli_chapter.py` for recovery, retry exhaustion, auth fail-fast, and invalid-request fail-fast behavior.
- Added the exact required E2E tests `test_chapter_draft_retry_recovery_preserves_single_write` and `test_chapter_draft_retry_exhaustion_leaves_no_partial_side_effects` in `tests/test_cli_e2e.py`.
- Verified Task 3 gates: `rtk py -3.12 -m pytest tests/test_cli_chapter.py -k "draft and (retry or rate_limit or provider)"` passed (`11 passed, 45 deselected`), and `rtk grep "def test_chapter_draft_retry_recovery_preserves_single_write|def test_chapter_draft_retry_exhaustion_leaves_no_partial_side_effects" "tests/test_cli_e2e.py" && rtk py -3.12 -m pytest tests/test_cli_e2e.py::test_chapter_draft_retry_recovery_preserves_single_write tests/test_cli_e2e.py::test_chapter_draft_retry_exhaustion_leaves_no_partial_side_effects` passed (`2 passed`).
- Landed Task 4 packaged verification changes under `changes/route-a-real-trigger-verification/` only: extended the offline harness with deterministic retry sequencing, added a packaged verifier runner, and generated recovery/exhaustion evidence plus isolated `runtime/retry-recovery/` and `runtime/retry-exhaustion/` lanes.
- Ran `rtk py -3.12 "changes/route-a-real-trigger-verification/support/task_4_packaged_resilience.py"`, which proved packaged `novel.exe chapter draft --chapter 1` recovers after one retryable failure with a single chapter write, and packaged plain/JSON exhaustion preserves the frozen `chapter draft failed after 3 attempts:` contract with zero partial writes.
- Repaired final-wave blockers in the active worktree only: removed root/generated clutter and cache directories, then added worktree-only packaged QA evidence for `novel.exe --help` plus missing-provider plain/JSON fail-fast behavior using worktree `PYTHONPATH`.
- Ran `rtk py -3.12 "changes/route-a-real-trigger-verification/support/task_4_worktree_packaged_preflight.py"`, producing `changes/route-a-real-trigger-verification/evidence/task-4-packaged-worktree-preflight.txt` from the active worktree path with no live network and no fallback to the main repo.
- Synced Route A readiness truth in the worktree docs only: updated `architecture-novel-runtime-v1.md`, `changes/reframe-roadmap-api-and-helper-cli/design.md`, and `changes/reframe-roadmap-api-and-helper-cli/route-matrix.md` to describe shipped provider + resilience + packaged verification proof instead of placeholder-only or planned wording.
- Verified the required stale-match grep returned zero hits for `Route A runtime entry .* planned|make-chapter-draft-llm-executable .* planned|Route A partial|placeholder scaffolding|placeholder evidence`.
- Correction: removed the last lingering `Route A partial` phrasing from `architecture-novel-runtime-v1.md` so the Task 5 truth docs now say `Route A ready` only, with scope guardrails intact.
